# Feature test for matching the UDP source and destination port

require = OpenFlow10

# OpenFlow
of_script = feature_test.sh, $ip, $port, $name

# Load-Generator
load_gen = moongen
files = feature_test.lua
lg_args = $file1, $name, $con=1, $con=2
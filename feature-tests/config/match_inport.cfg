# Feature test for matching of OpenFlow ingress port

require = OpenFlow10

# OpenFlow
of_args = $link=1, $link=2

# Load-Generator
load_gen = moongen
files = feature_test.lua, feature_config.lua
lg_args = $file=1, $name, $link=1, $link=2
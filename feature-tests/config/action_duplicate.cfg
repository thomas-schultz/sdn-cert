# Feature test for duplicating packets

require = OpenFLow10

# OpenFlow
of_script = feature_test.sh, $ip, $port, $name, $link=1, $link=2

# Load-Generator
load_gen = moongen
files = feature_test.lua, feature_config.lua
lg_args = $file1, $name, $link=1, $link=2